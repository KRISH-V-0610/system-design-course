# Week 0 — Setup & prerequisite honesty check

## Prerequisite honesty check

Do these WITHOUT looking anything up. Write your answer, then verify, then
note honestly whether you needed a hint.

- [ ] SQL: query with a self-join, a window function, and `GROUP BY ... HAVING`
- [ ] HTTP: explain URL-to-first-byte in 8+ distinct steps
- [ ] Concurrency: concurrency vs parallelism, and where Node sits
- [ ] Linux: what do `ps`, `top`, `lsof`, `netstat`/`ss`, `strace` each show
- [ ] Git: branch, rebase, resolve a conflict without panicking

## Setup

- [ ] `docker-compose.yml` up: Postgres + Mongo + Redis + Redpanda + Prometheus + Grafana + Tempo + Loki
- [ ] `sysdesign-notes` repo created (this one)
- [ ] `perf-lab` repo scaffolded with k6 + results dir
- [ ] Verified all containers healthy (`docker compose ps`)

## Notes

## Open questions

<!-- things I still can't explain cleanly — carry these forward -->

## Links

- Reading:
- Blog post of the week:
